public class main {
    public static void main(String[] args) {
        /*
        UI ui = new UI();
        financeManager financeManager = new financeManager();
        boolean applicationRunning = true;

        while (applicationRunning) {
            ui.displayLoginScreen();
            String username = ui.getUsername();
            String password = ui.getPassword();
            boolean loggedIn = financeManager.login(username, password);

            if (loggedIn) {
                ui.displayMainMenu();
                int option = ui.getMainMenuOption();

                switch (option) {
                    case 1:
                        //view account balance
                        break;
                    case 2:
                        // create savings goal
                        break;
                    case 3:
                        // deposit
                        break;
                    case 4:
                        // withdraw
                        break;
                    case 5:
                        // emergency unlock
                        break;
                    case 6:
                        // lock
                        break;
                    case 7:
                        //logout
                        financeManager.logout();
                        applicationRunning = false;

                        break;
                    default:
                        ui.displayErrorMessage("Invalid option. Please try again.");
                }
            } else {
                ui.displayErrorMessage("Invalid username or password. Please try again.");
            }
        }

         */
    }
}
